@model List<Model.EF.Menu>

<div class="call">
    <p><span>Bạn cần tư vấn ? </span> hãy gọi đến <span class="number">0378551027</span></p>
</div>

        <div class="account_desc">
            <ul>
                @foreach (var item in Model)
                {
                    <li><a href="@item.Link" target="@item.Target">@item.NameMenu</a></li>
                }
            </ul>
        </div>
        <div class="clear"></div>
